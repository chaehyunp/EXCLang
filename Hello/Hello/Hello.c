#include <stdio.h> //전처리기'#': 컴파일 전에 처리, <>꺽쇠: 헤더파일

/*화면출력
1. printf("출력Data");
2. printf("출력서식",출력Data);
3. printf("출력서식",변수이름);
*/

void main() //모든 함수는 메인함수에서 시작하고 메인함수에서 끝남, 맨 처음 호출되고 맨 나중에 종료됨
{
	printf("Hello Android!"); //모든 실행문이 끝나면 마침표, 세미콜론!
}